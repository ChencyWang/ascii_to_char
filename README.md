# ascii_to_char
windows console tool, convert ascii numeric value to character.

## usage
1. launch ascii_to_char.exe
2. input integer between 0-127
3. input -1 to exit

## features
- value range check (0 ~ 127)
- handle non-numeric input, avoid program stuck
- support continuous conversion
